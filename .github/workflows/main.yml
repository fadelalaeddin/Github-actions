name: My Workflow

on:
  push:
    branches:
      - main

jobs:
  example-job
    runs-on: ubuntu-latest

    steps: 
      - name: Print a welcome message
        run: echo "Welcome to my Github profile" 
        
